import React, { useContext } from 'react';
import { AuthContext } from '../Au/Authprovider';

const Drvide = ({ options }) => {

    return (
        <div className="divider">
            <h1 className='font-semibold  normal-case text-xl'>FIND YOUR OLD BOOK   </h1>
        </div>
    );
};

export default Drvide;